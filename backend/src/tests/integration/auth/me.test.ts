import { describe, it } from '@jest/globals';

describe('GET /api/auth/me', () => {
  it.todo('returns the current user for a valid access token');
  it.todo('rejects an expired or missing token');
});
