import { describe, it } from '@jest/globals';

describe('POST /api/auth/verify-otp', () => {
  it.todo('issues tokens for a valid, unexpired OTP');
  it.todo('rejects an expired or already-used OTP');
});
