import { describe, it } from '@jest/globals';

describe('GET /api/appointments/my-bookings', () => {
  it.todo('returns only the caller\'s own appointments');
  it.todo('requires authentication');
});
